# Runbook: Demo Scenarios

Scenario 1: Browser-based RDP via Guacamole
- Access HAProxy endpoint → Guacamole UI → login → connect to XRDP host

Scenario 2: NFS-backed user data
- Verify NFS mounts available on session hosts; persist files across sessions

Scenario 3: Minimal failover
- Demonstrate alternate session host availability via gateway selection

Notes:
- Capture screenshots and metrics as you iterate (Docs/Build-Log)
